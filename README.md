# generatedocs

## Usage

```
$> generatedocs.py extension_dir
```

Will generate `/extension_dir/script_api/extension_dir.script_api` and `/build/api.md` from annotations found in `/extension_dir/*.lua` and `/extension_dir/*.cpp`.
